# ZMBACKUP Patch & Audit Report — v1.2.13

**Date**: 2026-08-29  
**Target Version**: 1.2.13  
**Repository**: /Users/alsyundawy/Downloads/GitHub/zmbackup  
**Branch**: 1.2-version  
**Commit**: 866d530  
**Lead Maintainer**: Harry Dertin Sutisna Alsyundawy  

---

## 1. Executive Summary

This enterprise patch updates **ZMBACKUP** from v1.2.12 to **v1.2.13**. All changes focus on high-assurance security hardening, zero-downtime hot backup reliability, mitigation of critical CVE vectors, and POSIX portability across Linux distributions.

## 2. Key Security Mitigations

| Category | Component | Vulnerability / Risk | Solution Implemented |
|---|---|---|---|
| **RCE Prevention** | `MiscAction.sh` | SED BRE command injection via hostname | Replaced with pure AWK `index()` and `substr()` literal engine |
| **Credential Shield** | `ListAction.sh` | Password leakage in `/proc` and `ps` | Replaced `-w $LDAPPASS` with mode 0600 `-y $LDAP_PASS_FILE` |
| **Zip-Slip** | `RestoreAction.sh` | Path traversal archive extraction (CVE-2022-27925) | Integrated strict `verify_archive_safety()` validation |
| **Process Table** | `MiscAction.sh` | PID file spoofing & platform differences | Replaced fragile `ps` parsing with POSIX `kill -0` |
| **Argument Safety** | `MiscAction.sh` | Word-splitting & globbing on email lists | Replaced string split with `IFS=',' read -ra` |
| **CVE Proactive Alert** | `project/zmbackup` | CVE-2026-73570 (SNMP RCE) & CVE-2025-71275 | Added `check_cve_mitigation()` pre-flight checker |

## 3. Database Schema V2 & Relational Improvements

- Extended `backup_session` with metadata fields: `source_os`, `zimbra_version`, `manifest_hash`.
- Created composite index `idx_session_status` on `backup_session(status)`.
- Updated all test fixtures and migration code to named SQL columns.

## 4. Verification Evidence

- Bash Syntax Validation (`bash -n`): **PASSED (0 Errors)**
- ShellCheck Static Analysis: **CLEAN**
- Target Version Tag: **1.2.13**
- Git Remote Safety: **Guarded (0 push events)**

---
*Report automatically generated by patch_zmbackup.sh on 2026-08-29.*
